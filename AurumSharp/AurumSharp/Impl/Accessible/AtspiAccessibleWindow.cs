using System;
using System.Collections.Generic;
using System.Text;

namespace SharpTest.libsharptest.Impl.Accessible
{
    class AtspiAccessibleWindow : AccessibleWindow
    {
        public AtspiAccessibleWindow(AccessibleApplication app, AccessibleNode node) : base(app, node)
        {
            // nothing to do // 
        }
    }
}
